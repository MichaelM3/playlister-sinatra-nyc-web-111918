<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @artists.each do |artist| %>
    <ul><a href="<%= "/artists/#{artist.slug}" %>"> <%= artist.name %> </a></ul>
    <% end %>
  </body>
</html>
