<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @songs.each do |song| %>
      <ul><a href="<%= "/songs/#{song.slug}" %>"> <%= song.name %> </a></ul>
    <% end %>
  </body>
</html>
